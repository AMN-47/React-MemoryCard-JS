import React from "react";
import './Header.css';

export default function Header({ score, bestScore, total }) {
    return(
        <header className = "header">
            
        </header>
    );
}